- [x] Reset stats routing now runs through `admin_post_kdquiz_reset_stats` with `check_admin_referer` + capability guard, and the success notice verifies its own nonce before rendering (`includes/kd-quiz-settings.php:179`, `includes/kd-quiz-settings.php:204`).
- [x] Standardised every public identifier on the four-character `kdquiz` prefix (AJAX actions, shortcode, CPT, options, selectors) and migrated any legacy `kd_` data to the new names so WordPress.org’s uniqueness requirement is satisfied without breaking existing installs (`includes/kd-quiz-ajax.php:41`, `includes/kd-quiz-frontend.php:23`, `includes/kd-quiz-editing.php:84`).
- [x] JSON importer redirects back to the admin screen with nonce-protected status flags so both success and error notices are scoped correctly (`includes/kd-quiz-import.php:40`, `includes/kd-quiz-import.php:117`).
- [x] The “Mark as correct” pill now escapes its status text/attributes and persists the chosen answer after save; the status span is rendered with `esc_html()`/`esc_attr()` to satisfy the escaping sniff (`includes/kd-quiz-editing.php:198`).
- [x] Replaced the direct `$wpdb->update()` migration with a `get_posts()` + `wp_update_post()` loop, removing the discouraged direct SQL (and the `suppress_filters` flag) while keeping legacy posts in sync (`includes/kd-quiz-editing.php:84`).
